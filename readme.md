Run in DEV mode:
```
npm run dev test
npm run dev [file-name]
```

Build
```
npm run build
npm start test
npm start [file-name]
```

Simulator input file must be an `.xslx` file located in `data` folder.
